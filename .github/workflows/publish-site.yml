# .github/workflows/publish-site.yml
name: Build and Deploy Documentation

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 1. Checkout repository
        uses: actions/checkout@v4

      # ... (Steps 2, 2.1, 3 - Ruby, Jekyll gem, wkhtmltopdf install - unchanged) ...

      - name: 4. Build Jekyll Site
        working-directory: ${{ github.workspace }}
        run: |
          echo "Current directory for jekyll build: $(pwd)"
          jekyll build --source ./docs --destination ./docs/_site
          echo "Jekyll site built to docs/_site"

      # --- NEW DEBUG STEP HERE! ---
      - name: 4.5 Debug docs/_site after Jekyll build
        run: |
          echo "Listing contents of docs/_site after Jekyll build:"
          ls -R ./docs/_site || { echo "ERROR: ./docs/_site directory not found or empty after Jekyll build!"; exit 1; }
          echo "Size of docs/_site:"
          du -sh ./docs/_site || { echo "ERROR: Could not get size of ./docs/_site!"; exit 1; }
        working-directory: ${{ github.workspace }}


      - name: 5. Generate Documentation PDF
        run: |
          ./bin/assemble_docs_body.sh # Execute your script

          if [ -f "docs/pdfs/assembled.pdf" ]; then
            echo "PDF file (docs/pdfs/assembled.pdf) successfully created. Ignoring previous script exit code."
            exit 0
          else
            echo "ERROR: PDF file (docs/pdfs/assembled.pdf) was NOT created, and script failed. Propagating failure."
            exit 1
          fi

      - name: 6. Move PDF into _site for Deployment
        run: |
          mkdir -p docs/_site/pdfs
          cp docs/pdfs/assembled.pdf docs/_site/pdfs/assembled.pdf
          echo "PDF copied to docs/_site/pdfs/assembled.pdf"

      - name: 6.7 Run HTMLProofer on generated site
        # ... (htmlproofer step unchanged from last successful version) ...

      - name: 6.9 Debug _site contents before deploy
        # ... (debug step unchanged) ...
          
      - name: 7. Deploy _site to 'output' branch
        # ... (deployment step unchanged) ...
